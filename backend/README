-- add folder backend and frontend
-- cd backend
-- khỏi tạo ứng dung nodejs
-- npm init -y 
-- cài đặt express
-- npm install express@4.18.2
-- add server.js nơi khởi tạo app chứa thư mục gốc
-- run server 
-- npm run dev
-- install nodemon để tự động restart server khi có thay đổi
-- npm  install nodemon -D
-- cls  = clears
-- pass = zp8PGp6wv4aob6JG
-- connect DB 
mongodb+srv://leminhhiepco_db_user:zp8PGp6wv4aob6JG@cluster0.s2dpa3y.mongodb.net/?appName=Cluster0
 -- install mongoose
-- npm install mongoose
-- npm install dotenv


#  màu nền 
#  <div className="relative min-h-screen w-screen bg-white overflow-hidden">
#       {/* Soft Blue Radial Background */}
#       <div
#         className="absolute inset-0 z-0"
#         style={{
#           backgroundImage: `
#             radial-gradient(circle at top center, rgba(59, 130, 246, 0.5), transparent 70%)
#           `,
#           backgroundColor: "#ffffff",
#         }}
#       />

#       {/* Nội dung chính */}
#       <div className="relative z-10 flex items-center justify-center min-h-screen">
#         <div className="w-full max-w-md ">
#           // code ở đây

#         </div>

#       </div>
#     </div>